Latent Dirichlet Allocation is an unsupervised algorithm used mainly for topic modelling to assign categories to a corpus of documents automatically (without needing labels). It is a generalization of probabilistic latent semantic analysis. The resulting topics can then be used for things such as document retrieval, search engines and similar. Note that LDA is also an acronym for a different machine learning model called 'Linear Discriminant Analysis'. The original paper can be found here: [Latent Dirichlet Allocation](https://www.jmlr.org/papers/volume3/blei03a/blei03a.pdf)

Just check the paper. The algorithm itself is not too complicated, however, the parameter estimation is much harder to understand.
# Algorithm
Latent Dirichlet allocation (LDA) is a generative probabilistic model of a corpus. The basic idea is that documents are represented as random mixtures over latent topics, where each topic is characterized by a distribution over words. 

LDA assumes the following generative process for each document $\mathbf{w}$ in a corpus $D$ :
1. Choose $N \sim \operatorname{Poisson}(\xi)$.
2. Choose $\theta \sim \operatorname{Dir}(\alpha)$.
3. For each of the $N$ words $w_n$ :
	(a) Choose a topic $z_n \sim \operatorname{Multinomial}(\theta)$.
	(b) Choose a word $w_n$ from $p\left(w_n \mid z_n, \beta\right)$, a multinomial probability conditioned on the topic $z_n$
![[latent_dirichlet_allocation.png]]

We won't go over how to train LDA models, but LDA models are usually trained either using Gibbs Sampling or by using Variational Inference and Expectation-Maximization methods.